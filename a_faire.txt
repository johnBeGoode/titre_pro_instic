- Systeme interface admin:
	- appliquer le tab dynamique grafikart (bootstrap)
	- module ajouter article sur une autre page

- template ou mise en forme maison ??

- utiliser les diagrammes merise (mcd) et uml (diagramme de cas d'utilisation):

	- définition diag de cas d'utilisation: recence les gdes fonctionnalités
	d'un système (fonctions primaires). On ne s'attarde pas sur cmt on les fait.
	Un cas d'utilisation c'est un service rendu par le système en réponse
	à une sollicitation d'un acteur.

	- déf MCD: décrit les données du SI (entité-association). Normalement on
	fait un dictionnaires des données avant mais pr des questions de temps
	fait manuellement étant donné le nombre réduit de tables

- pagination si bcp de billets (optionnel)

L'utilisateur se connecte et peut poster ses commentaires (car il s'agit d'un blog et non
d'un forum donc pas de gestion des news)


